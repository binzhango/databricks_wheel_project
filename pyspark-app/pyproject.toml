[project]
name = "pyspark-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"

requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}

[project.scripts]
run = "pyspark_app.__main__:main"

